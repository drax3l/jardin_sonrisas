// public/js/home/index.js

document.addEventListener('DOMContentLoaded', () => {
    // Actualmente, toda la lógica de animaciones la maneja main.js.
    // Este archivo está listo para lógica exclusiva del Home (ej: un slider).
    console.log('🏠 Home JS cargado');
});