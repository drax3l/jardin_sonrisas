// src/public/js/admin/messages.js

document.addEventListener('DOMContentLoaded', () => {
    console.log('👮 Panel Admin cargado: messages.js activo');

    // Aquí pondremos lógica futura (filtros, botones de responder, etc.)
});